import { Container } from "inversify";

import { Module } from "../../app.module";
import { {{properCase name}}Controller } from "./{{dashCase name}}.controller";
import { {{properCase name}}Service } from "./{{dashCase name}}.service";

export class {{properCase name}}Module implements Module {
  Configure(ioc: Container) {
    ioc.bind({{properCase name}}Controller).to({{properCase name}}Controller).inSingletonScope();
    ioc.bind({{properCase name}}Service).to({{properCase name}}Service).inSingletonScope();
  }
}
